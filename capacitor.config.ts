import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.mealplan.helper',
  appName: '食谱小助手',
  webDir: 'dist',
  backgroundColor: '#f6f4f0',
  android: {
    backgroundColor: '#f6f4f0',
  },
}

export default config
